{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch  App",
        "type": "chrome",
        "request": "launch",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceFolder}",
        "preLaunchTask": "Start Server"
      }
    ]
  }